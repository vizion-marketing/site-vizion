# Images Groupe Aura

Placer ici les fichiers suivants (format AVIF de preference, sinon PNG) :

- logo_groupe-aura.png → logo du client (utilise dans les cartes et le listing)
- main.avif → image principale (hero de la page client, format paysage ~1200x675)
- fondateur.avif → photo de Thomas Ensenat (pour le temoignage)
- gallery/ → dossier optionnel pour les visuels de livrables (sites, campagnes, etc.)
