# Copier vers .env.local et renseigner les valeurs.
# Site (optionnel)
# NEXT_PUBLIC_SITE_URL=https://by-vizion.com

# Formulaire de contact (Resend)
# RESEND_API_KEY=

# --- Publication d'articles de blog par API (générateur externe) ---

# Secret partagé avec le générateur externe (obligatoire pour /api/blog/publish)
BLOG_PUBLISH_API_KEY=

# Token GitHub avec droit contents: write sur le dépôt (obligatoire)
GITHUB_TOKEN=

# Repo au format owner/repo (obligatoire), ou GITHUB_REPO_OWNER + GITHUB_REPO_NAME
GITHUB_REPO=

# Branche cible (optionnel, défaut: main)
# GITHUB_BRANCH=main

# URL du Deploy Hook Vercel pour déclencher un build après push (recommandé)
VERCEL_DEPLOY_HOOK_URL=
